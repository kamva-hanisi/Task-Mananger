# Backend:

- npm init -y
- npm install express
- npm install mysql
- npm install dotenv
- npm install cors
- npm install nodemon --save-dev

# `running: npx nodemon backend`

# Frontend:

- npx create-react-app client
- npm install axios

# `running: npm start`
